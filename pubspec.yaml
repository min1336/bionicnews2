name: bionic_news
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  html: ^0.15.4
  flutter_dotenv: ^5.1.0
  provider: ^6.1.2
  intl: ^0.19.0
  url_launcher: ^6.2.6 # ★★★ 웹 브라우저 실행을 위한 url_launcher 패키지 추가 ★★★

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env